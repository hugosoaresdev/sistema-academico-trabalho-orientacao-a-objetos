package org.example.app;

import javafx.stage.Stage;
import org.example.controller.AcademicSystemController;

public interface MenuApp {
    void carregarMenu(Stage stage, AcademicSystemController controller);
}
