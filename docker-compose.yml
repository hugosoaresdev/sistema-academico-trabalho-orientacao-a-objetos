services:
  academic-app:
    build: .
    container_name: sistema_academico_run
    environment:
      # No Windows com VcXsrv, o container alcança o X Server pela ponte host.docker.internal
      - DISPLAY=host.docker.internal:0.0
    extra_hosts:
      - "host.docker.internal:host-gateway"